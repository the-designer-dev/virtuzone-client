import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const sidebarLayout = ({children}) => {
  return (
    <View>
      <Text>sidebarLayout</Text>
    </View>
  );
};

export default sidebarLayout;

const styles = StyleSheet.create({});
